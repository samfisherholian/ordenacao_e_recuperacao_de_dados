A posicão atual compara com a proxima, se a posição atual for maior que o 
valor da proxima posição então ambos são trocados

bubblesort(A[], tamanho){

    para i <- 0 ate tamanho-1{

        
        troca <- 0
        // se a posição atual do array for maior que a proxima então ambos
        // trocam de posição
        para inteiro j <- 1 ate tamanho - k -1{

            se(A[j] > A[j + 1]) entao {

                temp <- A[j]

                A[j] <- A[j+1]

                A[j + 1] <- temp

                troca <- 1
            }

            se(troca = 0) entao {

                interrompa

            }

        }

    }


}